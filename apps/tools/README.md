# Tools

Useful `.rxe` utilities and `.rxr` app bundles.

- `system_pulse.rxe` - runs status, time, and storage checks.
- `rxr_inspector.rxe` - quick helper for verifying app bundles.
- `notes_plus.rxr` - bundled notes helper plus a LARDD help file.

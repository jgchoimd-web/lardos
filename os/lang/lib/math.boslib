; math.boslib - BOSL math utilities
; Include with: include "lib/math.boslib"

; clamp: (n lo hi -- v) clamp n to [lo,hi], i.e. min(max(n,lo), hi)
clamp:
    rot
    rot
    swap
    max
    swap
    min
    ret

; negate: (n -- -n) alias for neg
negate:
    neg
    ret

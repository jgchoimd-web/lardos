/*
 * compiler.seed - Self-hosting Seed compiler (skeleton).
 * This file is the Seed source of the compiler. When compiled by seedc (C bootstrap)
 * and run, it will eventually compile itself. For now it demonstrates the pipeline.
 *
 * Phase 1: Compiles minimal Seed (main, putint, expressions) to BOSL.
 * Phase 2 (future): Full self-hosting - this file compiles itself.
 */
int main() {
  putint(42);
  return 0;
}
